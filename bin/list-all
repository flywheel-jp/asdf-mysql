#!/usr/bin/env bash

set -euo pipefail

curl --fail -s https://downloads.mysql.com/archives/community/ |
  grep -E '>\s*(\d+\.\d+\.\d[^\>]*?)<\/option>' |
  sed 's|.*>\(.*\)<.*|\1|' |
  tr ' ' '-' |
  tac |
  tr '\n' ' '
